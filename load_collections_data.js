// insert pacientes
db.pacientes.insertMany( [
   	{ 
	   nombre: "Thor" 
	}
]);

db.pacientes.insertMany( [
   	{ 
	   nombre: "Wiskas" 
	}
]);
db.pacientes.insertMany( [
   	{ 
	   nombre: "Pelusa" 
	}
]);
db.pacientes.insertMany( [
   	{ 
	   nombre: "Brandy" 
	}
]);
