apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: pacientes
  name: pacientes
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pacientes
  template:
    metadata:
      labels:
        app: pacientes
    spec:
      containers:
      - image: docker.io/ecno20/cloud-paciente-service:1.0
        name: pacientes
        ports:
        - containerPort: 8084
          protocol: TCP
        resources:
          limits:
            cpu: '1'
            memory: 256Mi
          requests:
            cpu: '1'
            memory: 128Mi


---

apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: pacientes
  name: pacientes
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8083
  selector:
    app: pacientes
  type: ClusterIP
